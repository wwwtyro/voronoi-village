{
  "name": "voronoi-village",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "data:coastline": "node scripts/build-coastline.mjs",
    "data:cities": "node scripts/build-cities.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.70",
    "fflate": "^0.8.2",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "wgpu-matrix": "^3.4.2"
  }
}
